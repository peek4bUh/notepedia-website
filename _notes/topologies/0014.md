---
createdAT: "2024-09-13 17:33:22"
draft: "true"
id: "0014"
tags: [networking/topologies, computing]
title: Star Topology
---

# Star Topology

The devices are individually connected via a central networking device such as a switch or hub. This topology is the most commonly found today because of its reliability and scalability - despite the cost.

Any information sent to a device in this topology is sent via the central device to which it connects.

Because more cabling and the purchase of dedicated networking equipment is required for this topology, it is more expensive than any of the other topologies. However, despite the added cost, this does provide some significant advantages. For example, this topology is much more scalable in nature, which means that it is very easy to add more devices as the demand for the network increases.

Furthermore, the star topology is still prone to failure. For example, if the centralised hardware that connects devices fails, these devices will no longer be able to send or receive data. Thankfully, these centralised hardware devices are often robust.

![[star-topology.excalidraw|100%]]

## References

- _TryHackMe_. (2021, July 10). <span class="reference-title">Intro to LAN: Introducing LAN Topologies</span>. [tryhackme.com/introtolan](https://tryhackme.com/room/introtolan)

## Related

```dataviewjs
const results = dv.pages("#networking/topology")
  .where(p => p.draft === "false" && p.file.path !== dv.current().file.path)
  .sort(p => p.title)
  .map(p => p.title ? `[${p.title}](${p.file.path})` : p.file.link);

if (results.length > 0) {
  dv.list(results);
} else {
  dv.list(["No related notes found."]);
}
```
