---
draft: "false"
title: Run a Program at Startup via Registry
---

# Run a Program at Startup via Registry

## 1. Run as System-Wide

- Copy the Path of the Program that you want to run at Startup.
- `Windows + R` and type `regedit`.
- Navigate to `HKEY_LOCAL_MACHINE\Software\Microsoft\Windows\CurrentVersion\Run`.
- Right-click at an empty space and choose **New** > **String Value**.
- Name the new value with the name of the program you want to run.
- Now double click at the newly created value and at the Value data box, **Paste** (CRL + V) the copied path from the clipboard. When done, click **OK**.
- **Restart** your PC to apply the change.

## 2. Run as Current User

- Copy the Path of the Program that you want to run at Startup.
- `Windows + R` and type `regedit`.
- Navigate to `HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\Run`.
- Right-click at an empty space and choose **New** > **String Value**.
- Name the new value with the name of the program you want to run.
- Now double click at the newly created value and at the Value data box, **Paste** (CRL + V) the copied path from the clipboard. When done, click **OK**.
- **Restart** your PC to apply the change.

## References

- Tsoukalas, K. (2021, November 15). <span class="reference-title">How to run a program at startup via Registry</span>. _WinTips.org_. [https://www.wintips.org/how-to-run-a-program-at-startup-via-registry/](https://www.wintips.org/how-to-run-a-program-at-startup-via-registry/)
